generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url = "***"
}

model User {
  id     Int     @default(autoincrement()) @id
  name   String
  groups Group[]
  alarms Alarm[]
}

model Group {
  id     Int     @default(autoincrement()) @id
  name   String
  users  User[]
  alarms Alarm[]
}

model Alarm {
  id        Int     @default(autoincrement()) @id
  group     Group
  user      User
  message   String
}

model Track {
  id        Int     @default(autoincrement()) @id
}